<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Healthfy - Advanced AI-powered healthcare platform connecting patients with expert doctors and providing comprehensive medical solutions.">
    <meta name="keywords" content="healthcare, medical, doctors, AI diagnosis, telemedicine, health platform">
    <title><%= typeof title !== 'undefined' ? title : 'Healthfy - Advanced Healthcare Platform' %></title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='0.9em' font-size='90'>🏥</text></svg>">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- AOS Animation Library -->
    <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet">
    <!-- Bootstrap JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Advanced Custom CSS -->
    <style>
        :root {
            /* Professional Green Theme */
            --primary-gradient: linear-gradient(135deg, #10b981 0%, #059669 100%);
            --secondary-gradient: linear-gradient(135deg, #34d399 0%, #10b981 100%);
            --success-gradient: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
            --accent-gradient: linear-gradient(135deg, #6ee7b7 0%, #34d399 100%);
            --dark-gradient: linear-gradient(135deg, #064e3b 0%, #022c22 100%);
            --light-gradient: linear-gradient(135deg, #f0fdf4 0%, #ecfdf5 100%);
            
            /* Green Color Palette */
            --primary-green: #10b981;
            --secondary-green: #059669;
            --accent-green: #34d399;
            --light-green: #6ee7b7;
            --dark-green: #064e3b;
            --success-green: #22c55e;
            
            /* Professional Neutrals */
            --white: #ffffff;
            --cream-white: #fefefe;
            --light-gray: #f9fafb;
            --medium-gray: #e5e7eb;
            --dark-gray: #6b7280;
            --text-dark: #1f2937;
            --text-light: #6b7280;
            --text-muted: #9ca3af;
            
            /* Shadows */
            --shadow-sm: 0 2px 4px rgba(0,0,0,0.06);
            --shadow-md: 0 4px 6px rgba(0,0,0,0.1);
            --shadow-lg: 0 10px 25px rgba(0,0,0,0.15);
            --shadow-xl: 0 20px 40px rgba(0,0,0,0.2);
            
            /* Spacing */
            --border-radius: 12px;
            --border-radius-lg: 20px;
            --border-radius-xl: 30px;
        }

        * {
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: var(--text-dark);
            background: linear-gradient(135deg, var(--light-gradient), var(--white) 50%, var(--light-green));
            min-height: 100vh;
        }

        /* Enhanced Navigation */
        .navbar {
            background: var(--primary-gradient) !important;
            backdrop-filter: blur(15px);
            box-shadow: 0 8px 32px rgba(16, 185, 129, 0.25);
            padding: 1.2rem 0;
            position: sticky;
            top: 0;
            z-index: 1030;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }

        .navbar-brand {
            font-family: 'Poppins', sans-serif;
            font-weight: 900;
            font-size: 2rem;
            color: white !important;
            text-shadow: 0 2px 15px rgba(0, 0, 0, 0.3);
            letter-spacing: -0.5px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .navbar-brand:hover {
            transform: scale(1.05);
            color: var(--cream-white) !important;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
        }

        .navbar-brand i {
            color: var(--light-green) !important;
            -webkit-text-fill-color: var(--light-green) !important;
            animation: heartbeat 2s ease-in-out infinite;
            filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.3));
        }

        @keyframes heartbeat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        /* Search Bar Styling */
        .search-container {
            position: relative;
            width: 250px;
            max-width: 100%;
        }
        
        .search-input {
            padding-left: 2.5rem;
            border-radius: 25px;
            border: 2px solid var(--medium-gray);
            background: rgba(255, 255, 255, 0.9);
            transition: all 0.3s ease;
        }
        
        .search-input:focus {
            border-color: var(--primary-green);
            box-shadow: 0 0 0 0.2rem rgba(16, 185, 129, 0.25);
            background: white;
        }
        
        .search-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--dark-gray);
            pointer-events: none;
        }

        .navbar-nav .nav-link {
            font-weight: 600;
            color: white !important;
            padding: 0.8rem !important;
            border-radius: 50%;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            width: 3rem;
            height: 3rem;
            display: flex !important;
            align-items: center;
            justify-content: center;
            margin: 0 0.25rem;
        }

        .navbar-nav .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.25);
            transform: scale(0);
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: -1;
            border-radius: 50%;
        }

        .navbar-nav .nav-link:hover {
            color: var(--cream-white) !important;
            transform: translateY(-3px) scale(1.1);
            box-shadow: 0 8px 25px rgba(255, 255, 255, 0.3);
        }

        .navbar-nav .nav-link:hover::before {
            transform: scale(1);
        }

        .navbar-nav .nav-link.active {
            background: rgba(255, 255, 255, 0.2);
            color: var(--cream-white) !important;
            box-shadow: 0 8px 25px rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }

        .navbar-nav .nav-link i {
            font-size: 1.1rem;
            transition: transform 0.3s ease;
        }

        .navbar-nav .nav-link:hover i {
            transform: scale(1.1);
        }

        /* Notification Dropdown */
        .notification-dropdown {
            max-height: 400px;
            overflow-y: auto;
        }
        
        .notification-item {
            padding: 0.75rem 1rem;
            border-bottom: 1px solid var(--medium-gray);
            transition: all 0.3s ease;
        }
        
        .notification-item:hover {
            background: var(--light-gray);
            transform: none;
        }
        
        .notification-item:last-child {
            border-bottom: none;
        }
        
        .notification-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }
        
        .notification-title {
            font-weight: 600;
            font-size: 0.9rem;
            color: var(--text-dark);
            margin-bottom: 0.2rem;
        }
        
        .notification-text {
            font-size: 0.8rem;
            color: var(--text-light);
            margin-bottom: 0.2rem;
        }
        
        .notification-time {
            font-size: 0.75rem;
            color: var(--dark-gray);
        }

        .dropdown-menu {
            border: none;
            box-shadow: var(--shadow-lg);
            border-radius: var(--border-radius);
            padding: 0.5rem;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
        }

        .dropdown-item {
            border-radius: var(--border-radius);
            padding: 0.75rem 1rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .dropdown-item:hover {
            background: var(--primary-gradient);
            color: white;
            transform: translateX(5px);
        }

        .profile-avatar {
            width: 2.5rem;
            height: 2.5rem;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--light-gradient));
            border: 2px solid white;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            color: var(--primary-green);
            font-size: 1.1rem;
        }

        .profile-avatar:hover {
            border-color: var(--light-green);
            transform: translateY(-2px) scale(1.1);
            box-shadow: 0 8px 20px rgba(255, 255, 255, 0.4);
            background: rgba(255, 255, 255, 0.3);
            color: white;
        }

        .profile-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
        }

        /* Mobile Search */
        .mobile-search {
            display: none;
            padding: 1rem;
            border-top: 1px solid var(--medium-gray);
            background: white;
        }
        
        .mobile-search.show {
            display: block;
        }

        /* Enhanced Buttons */
        .btn {
            font-weight: 600;
            border-radius: var(--border-radius);
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
            border: none;
            position: relative;
            overflow: hidden;
        }

        .btn-primary {
            background: var(--primary-gradient);
            box-shadow: var(--shadow-md);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .btn-outline-primary {
            border: 2px solid var(--primary-green);
            background: transparent;
            color: var(--primary-green);
        }

        .btn-outline-primary:hover {
            background: var(--primary-gradient);
            border-color: transparent;
            color: white;
            transform: translateY(-2px);
        }

        /* Enhanced Cards */
        .card {
            border: none;
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow-md);
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(20px);
            overflow: hidden;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-xl);
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--primary-gradient);
        }

        /* Enhanced Alerts */
        .alert {
            border: none;
            border-radius: var(--border-radius);
            font-weight: 500;
            box-shadow: var(--shadow-md);
            backdrop-filter: blur(20px);
        }

        .alert-success {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(34, 197, 94, 0.1));
            border-left: 4px solid var(--success-green);
            color: var(--dark-green);
        }

        .alert-danger {
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(220, 38, 38, 0.1));
            border-left: 4px solid #ef4444;
            color: #991b1b;
        }

        .alert-info {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(52, 211, 153, 0.1));
            border-left: 4px solid var(--primary-green);
            color: var(--dark-green);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .navbar-brand {
                font-size: 1.5rem;
            }
            
            .navbar-nav .nav-link {
                padding: 0.5rem 0.75rem !important;
            }

            .btn {
                padding: 0.6rem 1.2rem;
                font-size: 0.9rem;
            }

            .search-container {
                width: 100%;
            }
        }

        /* Utility Classes */
        .bg-gradient-primary {
            background: var(--primary-gradient) !important;
        }

        .text-gradient {
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Page Transition */
        .page-transition {
            opacity: 0;
            transform: translateY(20px);
            animation: pageLoad 0.6s ease-out forwards;
        }

        @keyframes pageLoad {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Enhanced Focus States */
        *:focus {
            outline: 2px solid var(--primary-green);
            outline-offset: 2px;
        }

        /* Smooth Scrolling */
        html {
            scroll-behavior: smooth;
        }

    </style>
</head>
<body class="page-transition">

    <!-- Enhanced Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <i class="fas fa-heartbeat me-2"></i>
                Healthfy
            </a>
            
            <!-- Mobile Search Toggle -->
            <div class="d-lg-none me-2">
                <button class="btn btn-outline-secondary btn-sm" type="button" onclick="toggleMobileSearch()">
                    <i class="fas fa-search"></i>
                </button>
            </div>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'home' ? 'active' : '' %>" href="/" title="Home" data-bs-toggle="tooltip" data-bs-placement="bottom">
                            <i class="fas fa-home"></i>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'posts' ? 'active' : '' %>" href="/posts" title="Community" data-bs-toggle="tooltip" data-bs-placement="bottom">
                            <i class="fas fa-users"></i>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'doctors' ? 'active' : '' %>" href="/doctors" title="Find Doctors" data-bs-toggle="tooltip" data-bs-placement="bottom">
                            <i class="fas fa-user-md"></i>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'medicine-search' ? 'active' : '' %>" href="/medicine-search" title="Medicine Search" data-bs-toggle="tooltip" data-bs-placement="bottom">
                            <i class="fas fa-pills"></i>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'predictions' ? 'active' : '' %>" href="/prediction/xray" title="AI Diagnosis" data-bs-toggle="tooltip" data-bs-placement="bottom">
                            <i class="fas fa-brain"></i>
                        </a>
                    </li>
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'chat' ? 'active' : '' %>" href="/chat" title="Messages" data-bs-toggle="tooltip" data-bs-placement="bottom">
                                <i class="fas fa-comment-dots"></i>
                            </a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'video' ? 'active' : '' %>" href="/video-calls" title="Telemedicine" data-bs-toggle="tooltip" data-bs-placement="bottom">
                            <i class="fas fa-video"></i>
                        </a>
                    </li>
                    <% if (user && user.isDoctor) { %>
                    <li class="nav-item position-relative">
                        <a class="nav-link <%= typeof currentPage !== 'undefined' && currentPage === 'chat-requests' ? 'active' : '' %>" href="/doctors/chat-requests" title="Requests" data-bs-toggle="tooltip" data-bs-placement="bottom">
                            <i class="fas fa-calendar-check"></i>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" id="request-count" style="font-size: 0.6rem; display: none;">0</span>
                        </a>
                    </li>
                    <% } %>
                </ul>
                
                <!-- Search Bar -->
                <div class="d-none d-lg-flex me-3">
                    <form class="d-flex" role="search" onsubmit="performSearch(event)">
                        <div class="search-container">
                            <input type="search" class="form-control search-input" placeholder="Search doctors, posts, or topics..." aria-label="Search" id="globalSearch">
                            <i class="fas fa-search search-icon"></i>
                        </div>
                    </form>
                </div>
                
                <ul class="navbar-nav">
                    <% if (user) { %>
                        <!-- Notifications Bell -->
                        <li class="nav-item dropdown me-2">
                            <a class="nav-link position-relative" href="#" id="notificationDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" title="Notifications" data-bs-toggle="tooltip" data-bs-placement="bottom">
                                <i class="fas fa-bell"></i>
                                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" id="notification-count" style="font-size: 0.6rem;">
                                    3
                                    <span class="visually-hidden">unread notifications</span>
                                </span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end notification-dropdown" style="width: 320px;">
                                <li><h6 class="dropdown-header">Recent Notifications</h6></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item notification-item" href="#">
                                    <div class="d-flex">
                                        <div class="notification-icon bg-primary">
                                            <i class="fas fa-user-md text-white"></i>
                                        </div>
                                        <div class="flex-fill ms-2">
                                            <div class="notification-title">New Doctor Available</div>
                                            <div class="notification-text">Dr. Sarah Johnson is now accepting consultations</div>
                                            <div class="notification-time">2 hours ago</div>
                                        </div>
                                    </div>
                                </a></li>
                                <li><a class="dropdown-item notification-item" href="#">
                                    <div class="d-flex">
                                        <div class="notification-icon bg-success">
                                            <i class="fas fa-comments text-white"></i>
                                        </div>
                                        <div class="flex-fill ms-2">
                                            <div class="notification-title">New Message</div>
                                            <div class="notification-text">You have a new message in your consultation chat</div>
                                            <div class="notification-time">4 hours ago</div>
                                        </div>
                                    </div>
                                </a></li>
                                <li><a class="dropdown-item notification-item" href="#">
                                    <div class="d-flex">
                                        <div class="notification-icon bg-info">
                                            <i class="fas fa-heartbeat text-white"></i>
                                        </div>
                                        <div class="flex-fill ms-2">
                                            <div class="notification-title">Health Reminder</div>
                                            <div class="notification-text">Time for your weekly health check-in</div>
                                            <div class="notification-time">1 day ago</div>
                                        </div>
                                    </div>
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-center" href="/notifications">View All Notifications</a></li>
                            </ul>
                        </li>
                        
                        <!-- Quick Actions -->
                        <li class="nav-item dropdown me-2">
                            <a class="nav-link" href="#" id="quickActionsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" title="Quick Actions" data-bs-toggle="tooltip" data-bs-placement="bottom">
                                <i class="fas fa-plus-circle"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><h6 class="dropdown-header">Quick Actions</h6></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/posts/new"><i class="fas fa-pen me-2"></i>Create Post</a></li>
                                <li><a class="dropdown-item" href="/prediction/xray"><i class="fas fa-brain me-2"></i>AI Analysis</a></li>
                                <li><a class="dropdown-item" href="/medicine-search"><i class="fas fa-pills me-2"></i>Medicine Search</a></li>
                                <li><a class="dropdown-item" href="/doctors"><i class="fas fa-search me-2"></i>Find Doctor</a></li>
                                <% if (user.isDoctor) { %>
                                <li><a class="dropdown-item" href="/doctors/chat-requests"><i class="fas fa-calendar me-2"></i>View Appointments</a></li>
                                <% } %>
                                <li><a class="dropdown-item" href="/emergency"><i class="fas fa-exclamation-triangle me-2 text-danger"></i>Emergency</a></li>
                            </ul>
                        </li>
                        
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" title="<%= user.name %>" data-bs-toggle="tooltip" data-bs-placement="bottom">
                                <% if (user.profileImage) { %>
                                    <img src="<%= user.profileImage %>" class="profile-avatar" alt="<%= user.name %>">
                                <% } else { %>
                                    <div class="profile-avatar d-flex align-items-center justify-content-center">
                                        <i class="fas fa-user"></i>
                                    </div>
                                <% } %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><h6 class="dropdown-header"><%= user.isDoctor ? 'Dr. ' : '' %><%= user.name %></h6></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/users/dashboard"><i class="fas fa-tachometer-alt me-2"></i>Dashboard</a></li>
                                <li><a class="dropdown-item" href="/users/profile"><i class="fas fa-user me-2"></i>Profile Settings</a></li>
                                <li><a class="dropdown-item" href="/posts/new"><i class="fas fa-plus me-2"></i>Create Post</a></li>
                                <% if (user.isDoctor) { %>
                                <li><a class="dropdown-item" href="/doctors/dashboard"><i class="fas fa-hospital me-2"></i>Doctor Panel</a></li>
                                <% } %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-danger" href="/users/logout"><i class="fas fa-sign-out-alt me-2"></i>Logout</a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item me-2">
                            <a class="nav-link" href="/users/login" title="Login" data-bs-toggle="tooltip" data-bs-placement="bottom">
                                <i class="fas fa-sign-in-alt"></i>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="/users/signup" title="Sign Up" data-bs-toggle="tooltip" data-bs-placement="bottom">
                                <i class="fas fa-user-plus"></i>
                            </a>
                        </li>
                    <% } %>
                </ul>
            </div>
            
            <!-- Mobile Search Bar -->
            <div class="mobile-search" id="mobileSearch">
                <form class="d-flex" role="search" onsubmit="performSearch(event)">
                    <div class="search-container w-100">
                        <input type="search" class="form-control search-input" placeholder="Search doctors, posts, or topics..." aria-label="Search">
                        <i class="fas fa-search search-icon"></i>
                    </div>
                </form>
            </div>
        </div>
    </nav>

    <!-- Enhanced Flash Messages -->
    <% if (error && error.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show m-3" role="alert" data-aos="fade-down">
            <i class="fas fa-exclamation-circle me-2"></i>
            <strong>Error!</strong> <%= error %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>
    
    <% if (success && success.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show m-3" role="alert" data-aos="fade-down">
            <i class="fas fa-check-circle me-2"></i>
            <strong>Success!</strong> <%= success %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>
    
    <% if (typeof info !== 'undefined' && info && info.length > 0) { %>
        <div class="alert alert-info alert-dismissible fade show m-3" role="alert" data-aos="fade-down">
            <i class="fas fa-info-circle me-2"></i>
            <strong>Info:</strong> <%= info %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>

    <!-- JavaScript for Enhanced Header Features -->
    <script>
        // Toggle mobile search
        function toggleMobileSearch() {
            const mobileSearch = document.getElementById('mobileSearch');
            mobileSearch.classList.toggle('show');
        }
        
        // Global search functionality
        function performSearch(event) {
            event.preventDefault();
            const searchInput = event.target.querySelector('input[type="search"]');
            const query = searchInput.value.trim();
            
            if (query) {
                // You can customize this to redirect to a search results page
                window.location.href = `/search?q=${encodeURIComponent(query)}`;
            }
        }
        
        // Update notification count (you would call this from your backend)
        function updateNotificationCount(count) {
            const badge = document.getElementById('notification-count');
            if (badge) {
                badge.textContent = count;
                badge.style.display = count > 0 ? 'inline' : 'none';
            }
        }
        
        // Update request count for doctors
        function updateRequestCount(count) {
            const badge = document.getElementById('request-count');
            if (badge) {
                badge.textContent = count;
                badge.style.display = count > 0 ? 'inline' : 'none';
            }
        }
        
        // Initialize tooltips
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize all tooltips
            const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
            const tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl);
            });
            
            // Auto-hide alerts after 5 seconds
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(alert => {
                setTimeout(() => {
                    const bsAlert = new bootstrap.Alert(alert);
                    bsAlert.close();
                }, 5000);
            });
        });
        
        // Smooth scroll for anchor links
        document.addEventListener('DOMContentLoaded', function() {
            const anchorLinks = document.querySelectorAll('a[href^="#"]');
            anchorLinks.forEach(link => {
                link.addEventListener('click', function(e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({ behavior: 'smooth' });
                    }
                });
            });
        });

        
    </script>
